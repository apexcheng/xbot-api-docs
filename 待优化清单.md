# 待优化清单

本文档用于记录当前项目已确认、但暂不立即处理的优化项，避免后续重复讨论。

## 待办

### 1. 核心文档补来源标记

目标：让 Agent 或开发者能快速回溯关键结论对应的源码来源，减少猜测。

当前共识：

- 不能只写模糊来源
- 关键结论应尽量保留源码全路径定位能力
- 优先使用“小节级来源块”，避免正文过重
- 市场指令类内容除了源码路径，最好同时标注调用入口类型，如 `flow` / `direct python`

建议适用范围：

- `xbot-api-docs/docs/` 下的核心 API 文档
- `xbot-api-docs/docs/extension-instructions.md`
- `xbot-api-docs/docs/debug/market-extension-source.md`

## 已完成

### 1. 降低入口重复

目标：减少 `README.md`、`AGENTS.md`、`llms.txt` 之间的重复信息，降低维护成本。

建议分工：

- `README.md`：给人类读者看，说明项目是什么、适合谁、从哪里开始读
- `AGENTS.md`：只放 Agent 开发规则和仓库使用约定
- `llms.txt`：只保留给 LLM 的压缩索引和快速判断信息

已完成项：

- 根目录 `AGENTS.md` 作为当前知识库仓库唯一稳定主规则入口，`llms.txt` 只做索引和导航。
- `README.md`、根目录 `AGENTS.md`、`llms.txt` 的入口顺序已统一为“先遵循 `AGENTS.md`，再用 `llms.txt` / 子目录索引定位文档”。
- `xbot-api-docs/AGENTS.md` 已压缩为防漏规则，不再完整重复根目录规则。
- `xbot-api-docs/README.md` 和 `xbot-api-docs/docs/README.md` 作为人类阅读入口已删除，Agent 通过 `llms.txt` 和 `xbot-api-docs/llms.txt` 定位文档。

### 2. 删除历史分析结构

目标：避免历史扫描结果与稳定文档并列，导致 Agent 误读。

已完成项：

- `reference-analysis/` 中仍需验证的事项已迁入 `wiki/unresolved.md`。
- 代码风格和协作经验已合并到 `wiki/xbot-project-practices.md`。
- `reference-analysis/` 已删除。
- `xbot-api-docs/.gitignore` 已合并到根目录 `.gitignore` 并删除子目录 `.gitignore`。
- `wiki/xbot-project-collaboration-notes.md` 和 `wiki/xbot-project-guide-additions.md` 已合并为 `wiki/xbot-project-practices.md`。

### 3. examples 目录补齐示例

说明：

- `xbot-api-docs/examples` 现在已经有多份示例文件，不再是纯占位入口。
- 当前更适合把它视为已完成的基础示例集合。
- 如果后续继续补充，优先做高频的最小可运行示例，而不是一次性铺开。

建议优先顺序：

1. 浏览器打开页面并查找元素
2. 中文输入与 `clipboard_input()`
3. 下载文件并等待完成
4. Excel 批量写入二维数组
5. 市场指令 `flow` / `direct python` 调用示例

### 4. 大文档拆分

目标：降低超长文档的阅读和维护成本。

已完成项：

- `xbot-api-docs/docs/extension-instructions.md` 已保留为市场指令总入口，包含目录总览、指令映射、通用调用规则、拆分页导航、证据引用和快速查询。
- 各市场指令的详细参数、调用示例和注意事项已按指令拆到 `xbot-api-docs/docs/extensions/`。
- `xbot-api-docs/docs/browser.md` 内容较长但结构清楚，本轮暂不拆分；如后续需要，优先把模板和排错内容拆到独立 recipes 文档。
