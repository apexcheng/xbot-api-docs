# AGENTS.md

本文件是影刀 xbot / 编码版项目的通用 Agent 规则，适用于后续多个影刀项目复用。

核心目标：**最小改动、简单直接、不猜 API、不做无关重构。**

---

## 0. 执行优先级

1. 用户当前需求优先。
2. 当前项目已有代码和现有风格优先。
3. 本文件规则优先于 README、llms.txt 和普通文档。
4. API、市场指令参数、页面行为不确定时，才查知识库或源码。
5. 规则冲突时，以“改动更小、结构更简单、风险更低”为准。

---

## 1. 默认执行原则

1. 目标清楚时直接改，不要先写长计划。
2. 只解决当前任务明确要求的问题。
3. 只改与当前需求直接相关的文件、函数和代码行。
4. 不做无关重构、无关格式化、无关改名或结构重排。
5. 能用现有结构解决，就不要新增框架、管理器、抽象层或复杂配置。
6. 能从当前代码确认用法，就不要额外查知识库。
7. 发现无关潜在问题可以指出，但不要擅自修改。
8. 每一处改动都必须能对应当前需求。

---

## 2. 复杂任务处理

只有出现以下情况时，才按复杂任务处理：

1. 需求不清楚，需要先梳理目标或取舍。
2. 需要新增完整功能、流程、数据结构或配置。
3. 需要跨多个模块修改，并会影响主流程。
4. 涉及新的影刀 API、市场指令、外部系统或页面行为，且当前代码无法确认用法。
5. 涉及批量写入、删除、迁移、发布、权限、生产配置等高风险操作。
6. 用户明确要求先评审、先设计、先规划或先给方案。

复杂任务只做必要步骤：

1. 先给简短计划：目标、修改范围、风险点。
2. 先看当前项目已有代码，再决定是否查知识库或 API 文档。
3. 只搜索与当前目标直接相关的文件和调用。
4. 优先在现有结构内完成，不默认重构架构。
5. 分步骤修改，但不要为了流程拆得过细。
6. 不确定 API、页面行为、XPath、市场指令参数时，标注“需运行验证”。

---

## 3. 知识库使用规则

真实业务代码应在真实影刀项目目录中修改，不要把知识库目录当成业务项目根目录。

只有以下情况才查知识库或 `xbot-api-docs`：

1. 本次修改涉及新的影刀 API。
2. 本次修改涉及 Excel、浏览器、网页自动化、市场指令参数等不确定用法。
3. 当前项目已有代码无法确认 API 用法。
4. 用户明确要求查文档。

查询顺序：

1. 当前项目已有代码。
2. `xbot-api-docs/docs/`。
3. 相关示例或市场指令源码说明。
4. 必要时用 `inspect.signature()`、`inspect.getfile()` 看真实实现。

如果仍不确定，必须写“需运行验证”，不要自行推断。

---

## 4. 代码风格

1. 代码要简单、直接、容易阅读。
2. 优先顺序式流程，数据流显式，关键状态清楚。
3. 保持项目现有风格，不要为了“更规范”强行重构。
4. 变量名要直白，避免 `x`、`tmp`、`obj` 这类含义模糊的命名。
5. 默认使用标准库；新增第三方依赖需要用户明确允许，或项目已在使用。
6. 关键业务判断可以写中文注释；简单赋值、基础语法、显而易见的逻辑不要写注释。
7. 函数注释使用 Sphinx / reStructuredText 格式。
8. Git 提交信息、阶段性备注和变更摘要优先使用中文。

---

## 5. 函数、常量与结构

1. 默认不要为了“看起来规范”拆函数。
2. 单次调用、逻辑很短、内联更直观时，直接写在调用处。
3. 函数体只有一两行时，默认不要封装。
4. 抽函数后参数更多、阅读更绕时，不要抽。
5. 只有逻辑较长、明显重复、代表明确业务步骤，或项目已有同类结构时，才抽函数。
6. 默认不要新增类、管理器、工厂函数、复杂配置层或抽象层。
7. 默认不要为了“看起来像常量”就提取常量。
8. 单次使用的 XPath、CSS、URL、字段名、按钮文案、状态值、提示语，默认就近写在使用处。
9. `run.py` 保持简洁，只放主流程和必要调度。
10. 工具方法、复用逻辑和辅助函数优先放到 `tools.py`；简单单次逻辑直接内联。

---

## 6. 输入、参数与异常

1. 影刀项目入口通常为 `main(args)`。
2. 已知 `args` 参数名时，严格按已有参数名使用。
3. 新增 `args` 参数时，使用最小、直观、稳定的参数名；最终回复列出参数名、含义和建议类型。
4. 来自 `args` 的参数默认按“参数优先、空值回退全局变量”处理：`args.get("xxx") or glv.get("xxx")`。
5. 不要同时兼容多个猜测参数名，例如 `data_list / items / 数据列表` 混用。
6. 调用方已经约定入参类型或记录结构时，业务逻辑直接按约定使用。
7. 输入形态不确定时，只在边界统一归一化一次，不要在业务逻辑里反复兜底。
8. 异常处理只覆盖当前业务明确需要处理的失败情况。
9. 捕获异常时必须保留关键错误信息，不要静默吞异常。
10. 清理、关闭、停止监听等收尾失败，至少记录 `warning`。

---

## 7. 影刀日志与页面自动化

1. 影刀项目禁止使用 Python 标准 `logging`。
2. 只允许使用 `from xbot.app import logging`。
3. 日志方法只传一个字符串参数，例如 `logging.info(text)`。
4. 动态内容使用 f-string，例如 `logging.info(f"行数={n}")`。
5. 需要异常堆栈时，在 `except` 里用 `traceback.format_exc()` 拼到 `logging.error(...)`，不要用 `logger.exception(...)`。
6. 涉及浏览器、URL、页面业务时，优先使用 `xbot.web` 和浏览器对象能力。
7. 除非用户明确要求接口方式，或项目已有接口调用方式，否则不要用 `requests`、`httpx`、`aiohttp`、`urllib` 处理网页业务。
8. 能用元素、选择器、页面接口处理，就不要优先坐标点击。
9. 中文或长文本输入优先考虑剪贴板输入。
10. 页面字段、按钮含义、价格口径必须以当前需求、页面证据或已验证结果为准。
11. XPath、CSS、页面接口、按钮状态未验证时，必须标注“需运行验证”。

---

## 8. 修改与验证

1. 修改已有文件前，先阅读相关上下文，避免覆盖用户已有改动。
2. 手工改文件时做最小 diff，避免整文件无意义重写。
3. 不要执行会丢改动的命令，例如 `git reset --hard`、`git checkout --`，除非用户明确要求。
4. 搜索文件、代码和文本时优先用 `rg`。
5. 默认不新增测试，不默认运行完整测试。
6. 只有用户明确要求、命令很小且和本次修改直接相关，或不验证会有明显风险时，才运行必要检查。
7. 默认不主动运行 `shadowbot_sync_tool.py prepare`。
8. 只有用户明确要求同步、交付影刀编辑器可运行同步结果，或任务本身包含“同步 / prepare / 发布到影刀”时，才运行 `shadowbot_sync_tool.py prepare`。
9. 同步完成不等于已在影刀编辑器内验证通过。
10. 未运行测试或同步时，最终回复必须明确说明。

---

## 9. 禁止行为

1. 无关重构、无关格式化、无关改名。
2. 擅自更换实现路线。
3. 引入不必要依赖。
4. 引入复杂封装、复杂抽象层、复杂配置系统。
5. 为低概率场景写大量防御代码。
6. 隐藏副作用或吞异常不说明。
7. 基于旧项目经验猜 API、命令、目录、文件、XPath、页面字段、按钮含义或价格口径。
8. 同时兼容多个未经确认的参数名。
9. 自行猜测市场指令编码版参数。
10. 提交账号、密码、token、webhook、敏感路径。
11. 把“需运行验证”的内容描述成“已经验证通过”。

---

## 10. 最终回复

完成后简短说明：

1. 改了哪些文件。
2. 核心改动是什么。
3. 是否运行测试 / 同步。
4. 是否还有需运行验证或需人工确认的内容。

只有遇到阻塞、测试失败、不确定 API 或需要用户决策时，才写详细说明。
