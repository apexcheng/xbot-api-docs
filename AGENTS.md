# AGENTS.md

本文件是本知识库唯一的稳定 Agent 规则源。API 签名、参数和返回值属于 `xbot-api-docs/`，不要复制到这里。

核心原则：**最小改动、过程清楚、不猜 API、按风险验证。**

## 1. 优先级与边界

1. 用户当前需求优先。
2. 真实影刀项目中的业务逻辑、参数约定和数据口径优先保留。
3. 只修改与当前需求直接相关的文件和代码；不做无关重构、格式化、改名或结构调整。
4. 本仓库是知识库，不是真实影刀应用目录；业务代码应在用户指定的真实项目中修改。
5. README 和 `llms.txt` 只负责介绍与导航，不重复维护规则。
6. 现有代码可以提供线索，但不能单独证明 API 或业务行为正确。

## 2. 默认执行方式

1. 目标清楚时直接执行，不先写长计划或反复确认。
2. 能在现有结构内完成，就不要新增文件、依赖、配置层或抽象层。
3. 每一处改动都应能追溯到当前需求。
4. 发现无关问题可以简短指出，但不要擅自扩大修改范围。
5. 不创建任务状态文件、临时进度文档或为单次任务设计管理流程。

复杂任务只采用：

1. 简短计划：说明目标、修改范围和主要风险。
2. 执行：先读相关代码和文档，再集中完成必要修改。
3. 验证：按风险检查主要路径；失败时再缩小范围排查。

复杂任务包括跨模块功能、数据迁移、批量写入、删除、发布、权限或生产配置等高风险变更。

## 3. 代码结构

1. 影刀 RPA、自动化脚本和数据处理默认采用自上而下的过程式主流程。
2. 业务顺序应尽量直接可见，例如：读取 → 判断 → 操作 → 写回 → 保存 → 通知。
3. 函数、模块或类只在明显复用、形成独立业务边界或能显著降低理解成本时引入。
4. 单次使用的短逻辑优先就地表达，不为形式上的复用拆函数。
5. 默认不新增 Service、Manager、Context、Registry、Factory、Dispatcher、Strategy、多层 wrapper 或大型配置系统。
6. 不为未来可能出现的需求预留未使用参数、分支、Hook、兼容层或扩展点。
7. 状态只保留跨步骤确实需要共享的部分，避免巨型 `context` 或隐藏执行顺序的动态调度。
8. 异常处理只覆盖明确需要处理的失败；保留关键错误信息，不静默吞异常。

## 4. API 与知识库

1. 不根据通用 Python 经验、旧项目记忆或名称相似度猜 xbot API、市场指令、页面字段、XPath 或命令。
2. 当前代码已经能确认稳定用法时，优先沿用最小必要部分。
3. 无法确认时，按 `llms.txt` 直达对应的 `xbot-api-docs/` 页面，不做无边界搜索。
4. 文档仍不足时，检查可用源码、函数签名或真实运行证据。
5. 必须依赖影刀、WPS、浏览器或真实页面才能确认的行为，明确标记“需运行验证”，不得描述为已验证。
6. 知识库路径由用户或项目提供；路径缺失时请求正确位置，不猜固定绝对路径。
7. 影刀项目日志使用 xbot 日志能力，不使用 Python 标准 `logging`；网页业务默认使用 `xbot.web`，除非用户明确要求接口方式或项目已有稳定接口实现。

## 5. 修改与验证

1. 修改前阅读相关上下文，保留用户已有改动，不执行会丢失改动的命令。
2. 文案、配置或单文件小改只做相关检查，不默认新增测试或运行完整构建。
3. 多文件、公共逻辑或数据处理改动应运行相关测试，并检查主要调用方。
4. 删除、迁移、批量写入、发布、权限和生产配置应先确认目标与范围，并保留可回退方式。
5. 验证失败时先区分代码、环境、依赖、路径和无关测试问题，不为通过无关检查扩大改动。

## 6. 影刀同步

1. `shadowbot_sync_tool.py` 应位于真实项目根目录，与 `package.json` 同级。
2. 真实项目新增 `.py` 文件后，默认运行 `python shadowbot_sync_tool.py`；用户明确要求暂不同步时除外。
3. 用户说“同步影刀”“执行影刀同步”或同类表述时，就是在真实项目根目录执行该命令，即使本次只修改已有 `.py` 文件。
4. 同步不是 Git 提交、推送或普通文件复制，不能用其他操作代替。
5. 同步成功只说明文件已登记并通过脚本处理，不等于已在影刀编辑器中运行验证。

## 7. 安全与汇报

1. 不提交账号、密码、Token、Cookie、Webhook、客户数据或未公开地址；示例必须脱敏。
2. 不把推测、静态检查或同步结果表述成真实环境验证。
3. 完成后简短说明修改文件、核心变化、验证与同步情况。
4. 只有失败、阻塞、风险或需要用户决策时才展开说明。
