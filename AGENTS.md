# AGENTS.md

本文件是当前知识库仓库的唯一稳定 Agent 主规则入口。Codex、Claude Code、Hermes/OpenClaw 等 AI Agent 进入本仓库时，应优先遵循本文件。

本仓库是影刀 xbot / AI 开发知识库，不是真实影刀项目根目录。实际开发影刀项目时，Agent 应打开真实影刀项目根目录工作。

`templates/` 只保存可选的通用 Agent 规则模板，不参与本知识库检索和规则加载。

## 关键工具提醒

1. Agent 在真实影刀项目目录完成开发或修改后，必须运行本仓库根目录的 `shadowbot_sync_tool.py`。
2. `shadowbot_sync_tool.py` 默认位于当前知识库根目录，不在真实影刀项目目录里。
3. 不要假设当前工作目录就是知识库根目录；调用时要明确区分工具路径和 `--project-dir` 指向的真实影刀项目目录。
4. 最小调用示例：

```powershell
python C:\Users\Administrator\Desktop\影刀xAI开发指南\shadowbot_sync_tool.py --project-dir "%LOCALAPPDATA%\ShadowBot\users\<user_id>\apps\<app_id>" prepare main.py
```

## 使用顺序

1. 先遵循本文件规则；本文件是当前知识库仓库的稳定规则入口。
2. 再看 `llms.txt`，了解项目总览、目录结构和文档入口。
3. 进入 `xbot-api-docs/` 后，再看该目录下的 `AGENTS.md` 和 `llms.txt`。
4. 任务涉及影刀 API、xbot 指令、开发流程或文档维护时，Agent 应主动读取对应文档；`llms.txt` 不是所有 Agent 都会自动读取。

## Agent 执行检查清单

开发前：

1. 确认当前工作目录是真实影刀项目目录，还是本知识库目录。
2. 如果要写影刀编码版代码，先查 `xbot-api-docs/docs/` 对应文档。
3. API、页面行为、市场指令参数不确定时，先查文档或源码；仍不确定就标注“需运行验证”。
4. 先明确本次代码应采用的最简单流程写法；除非当前代码已有模式或需求明确要求，不设计新抽象。

修改前：

1. 明确当前问题、证据来源、应改位置和不可改范围。
2. 只做最小可行修改，不做无关重构、格式化或提前优化。
3. 浏览器、URL、网页业务功能默认使用 `xbot.web`，不要擅自改用网络请求库。
4. 对照“代码风格门禁”检查方案，发现过度封装时先降级为顺序式实现。

收尾前：

1. 删除本次改动造成的不再使用的 import、变量或函数。
2. 说明验证方式；不能在影刀编辑器内验证时，不要写成“已验证通过”。
3. 真实影刀项目中修改 `.py` 后，运行 `shadowbot_sync_tool.py prepare` 同步收尾。
4. 复核本次 diff：每个新增函数、常量、异常处理、兜底分支都必须能对应当前明确需求。

## 代码风格门禁

写代码前先做以下判断；只要答案是否定，就不要写对应结构：

1. **要不要新函数**：默认不要为了“看起来规范”拆函数。某段逻辑只在一个地方使用时，优先直接内联写在调用处，不要单独封装。只有在代码量较多、内联后明显影响主流程阅读，逻辑本身较复杂、需要单独命名降低理解成本，或后续已经明确会复用时，才考虑抽函数。一个函数如果只被引用 1 次，默认不要封装；如果函数体只有 1 条语句，默认也不要封装。
2. **要不要新常量**：只有多处复用、语义属于公共配置、或提取后明显更清楚时才提常量；单次 URL、字段名、XPath 默认写在使用处。
3. **要不要新类 / 管理器**：默认不要。除非现有项目已经用同类结构，或状态复杂到函数无法清楚表达。
4. **要不要配置层**：默认不要。当前任务只用一次的参数直接写在调用处或从既有 `args` / `package.variables` 读取。
5. **要不要兼容多种输入**：调用方已有约定时，直接按约定结构使用；不要反复写 `isinstance`、`dict(...)`、空 dict / 空 list 兜底。
6. **要不要传整个容器**：函数优先接收自己实际需要的具体值；如果只用 dict 里的少数字段，不要把整个 dict 传进去再拆。
7. **要不要映射层**：展示值和业务值本来一致时，不要额外加翻译函数、映射表或间接层。
8. **要不要提前分层**：不要为了单次遍历构造中间分层结构；后续流程不需要的结构不要提前设计。
9. **要不要捕获异常**：只有当前业务需要补充上下文、通知、截图或恢复流程时才捕获；捕获后必须保留关键错误信息，不要吞异常。
10. **要不要重试 / 多候选兜底**：默认不要。只有页面或接口已确认存在这种不稳定性时才加。
11. **要不要顺手改名 / 格式化 / 重排代码**：默认不要。只改当前需求直接相关的行。

反模式：

```python
PRICE_FIELD = "价格"  # 只用一次，不要提常量

class PriceCollector:  # 单个流程不需要管理器
    ...

def normalize_record(record):
    if not isinstance(record, dict):
        return {}
    return dict(record)

def get_price_text(card):
    return card.get_text()  # 只调一次、只有一条语句，不要单独封装
```

推荐：

```python
fields = record["fields"]
price = fields.get("价格")
```

## 使用原则

1. 写影刀编码版代码前，优先查 `xbot-api-docs`。
2. 不确定 API 行为时，不要猜，标注“需运行验证”。
3. 市场指令参数不明确时，先按 `xbot-api-docs/docs/debug/market-extension-source.md` 的方法排查源码。
4. 价格采集优先使用 XPath、CSS、页面接口或明确规则取数，不要让 Agent 自由猜页面价格。
5. 涉及浏览器、URL、网页业务功能时，除非用户明确要求，不要使用 `requests`、`httpx`、`aiohttp`、`urllib` 或其他网络相关 Python 库；只使用 `xbot.web` 及其浏览器对象能力处理页面、请求、Cookie、网络监听和下载。
6. 代码优先保证可读性，变量名、流程和判断逻辑要直观。
7. 阅读性优先于工程化；不要过度工程化，不要为了简单任务引入复杂封装、抽象层、配置系统或通用框架。
8. 只基于当前业务、当前代码、当前页面和当前需求修改。
9. 不要优化未知问题，不要提前处理未出现、未验证、低概率的场景。
10. 不要做无关重构，不要改动与当前任务无关的代码、格式、命名或结构。
11. 修改代码时优先做最小可行改动，确保每一处改动都能对应当前需求。
12. 如果发现潜在问题，可以指出，但不要擅自扩展修改范围。
13. 页面字段、价格口径、按钮含义必须以当前业务规则为准，不要自行推断。
14. 可视化界面能跑不代表编码版参数一致，编码版参数必须以文档或源码为准。
15. 能用元素操作就不要优先坐标点击。
16. 中文或长文本输入优先考虑剪贴板输入。
17. 表格大量写入时，优先考虑二维数组批量写入。
18. 不要提交影刀运行包源码、反编译源码、敏感路径、账号信息或 token。
19. 不要编造不存在的功能、命令、目录、文件或 API。
20. 钉钉 AI 表格记录默认按 `record["fields"]` 取值；读取记录列表时先走 `data` 层，不要直接对顶层结果 `.get("records")`。
21. 遇到多选字段时，先按 `{"name": "...", "id": "..."}` 结构理解显示值，但真正开发前必须实测字段真实结构，不要凭经验假设字段格式。

## 代码风格要求

1. 代码要简单、直接、容易阅读。
2. 优先顺序式流程，默认不要为了“看起来规范”拆函数。
3. 某段逻辑只在一个地方使用时，优先直接内联写在调用处，不要单独封装；原则是少封装、少抽象，让代码保持简单、直接、容易读。
4. 只有在代码量较多、内联后明显影响主流程阅读，逻辑本身较复杂、需要单独命名降低理解成本，或后续已经明确会复用时，才抽函数；只调用 1 次的函数默认不抽，只有 1 条语句的函数默认不抽。
5. 单次使用的常量、URL、固定字符串、小函数、小表达式默认直接写在使用处，不要因为“看起来像常量”就单独提取。
6. 只有在多处复用、语义上属于公共配置、或提取后可读性明显更好时，才允许提取常量或小函数。
7. 调用方已明确约定入参类型或记录结构时，被调函数直接按约定使用；不要在业务逻辑中重复写 `isinstance`、`dict(...)`、空结构回退这类防御性归一化。
8. 如果输入形态确实不确定，只允许在边界统一归一化一次；进入业务逻辑后，不要继续重复判断和兜底。
9. 反例：只引用一次的 URL 先提成常量再立刻使用。正例：直接在使用处内联，除非它确实会被复用。
10. 反例：调用方已保证是 `dict`，函数内部仍写 `isinstance(..., dict)` 再决定是否回退。正例：直接按既定结构取值。
11. 函数返回值优先只承载当前方法的主业务结果或不可逆的现场观测，不要为了后续计数、日志、汇总、通知等收尾步骤，顺手夹带额外统计信息。
12. 如果这些后置信息可以从主业务结果现场计算得到，就在最终使用处再算，不要提前耦合到当前函数的返回值里。
13. 只有当后置所需信息无法从主业务结果可靠推导，或现算会丢失必要上下文时，才考虑额外返回辅助统计，并明确保留原因。
14. 不要默认添加复杂异常体系、重试框架、日志框架或配置层。
15. 异常处理只覆盖当前业务中明确需要处理的失败情况。
16. 数据写入类任务要让失败隔离粒度和落盘时机对齐：逐项捕获异常就逐项保存，整批统一处理才在批量末尾统一保存。
17. 注释只解释业务关键点、特殊页面逻辑或不直观的 API 行为，不要写无意义注释。
18. 保持现有项目风格，不要无关格式化整个文件。
19. 涉及“业务处理 + 通知汇总”时，优先让业务模块返回结果列表，由入口流程统一收集，通知模块再做分组和格式化。
20. 通知结果列表建议使用 `notify_records` 这类直观命名，避免和表格回写 `records` 混淆。
21. 生成代码后必须回看一遍：如果删掉某个抽象、常量或兜底分支后代码仍然清楚，就删掉它。
22. 如果一个函数只被引用 1 次，优先内联回主流程；如果一个函数只有 1 条语句，优先直接写在使用处。短函数且不代表明确业务步骤时，优先内联，阅读性比“看起来更工程化”更重要。

## 代码协作约定

1. 代码关键步骤必须写中文注释，用来解释业务意图、关键判断和关键流程。
2. 不需要给每一行代码写注释，简单赋值、明显逻辑、小表达式不用强行注释。
3. Git 提交信息必须使用中文，`git commit` 的 message、阶段性备注和变更摘要都优先用中文，不要默认写成英文。
4. `run.py` 作为主入口文件，应保持简洁，只放主流程和必要调度逻辑；工具方法、复用逻辑和辅助函数优先放到 `tools.py`，如果项目中没有 `tools.py`，需要时再创建。
5. 不要把大量业务工具函数堆在 `run.py`，但也不要为了单次使用的小逻辑过度拆函数，简单逻辑可以直接内联在当前流程中。
6. 单文件较长且函数较多时，可以用中文分块注释做导航，但不要借机重排无关代码。

## 排查与修改原则

处理 bug 或异常时，按以下顺序：

1. 先确认当前问题。
2. 再找当前代码中的确定证据。
3. 只修改与问题直接相关的位置。
4. 修改后说明验证方式。
5. 不要把其他项目、旧经验、未确认框架行为套进当前问题。

如果 API、参数、页面行为不确定：

1. 先查 `xbot-api-docs`。
2. 再查当前项目已有代码。
3. 必要时用 `inspect.signature()`、`inspect.getfile()` 查看真实实现。
4. 仍不确定时，明确标注“需运行验证”。

## 市场指令排查规则

当市场指令文档不清楚、参数不明确、可视化能跑但编码版异常时：

1. 先用 `inspect.signature()` 查看包装层参数。
2. 再用 `inspect.getfile()` 定位 `xbot_extensions` 安装目录。
3. 查看 `__init__.py`、`_core.py` 或其他真实实现文件。
4. 以源码里的真实判断逻辑为准。
5. 不要根据界面中文选项猜编码版参数。
6. 若源码判断 `close_type == click`，编码版就应传 `click`，不是 `点击`。
7. 不要把所有市场指令都预设成返回 `dict`；返回值结构按该指令的文档、源码或实测契约判断，再决定怎么封装和校验。

## 源码参考路径

- 内置指令源码：`C:\Program Files\ShadowBot\shadowbot-6.0.30\Resources\Code-Activity\Zh-CN\xbot`
- 已安装的市场指令：`%LOCALAPPDATA%\ShadowBot\users\<user_id>\apps\<app_id>\xbot_extensions`
- `<user_id>` 和 `<app_id>` 以当前影刀用户和当前应用目录为准；不要把固定账号目录当作通用路径。

## 影刀项目协作流程

1. 影刀项目由人类先在影刀编辑器中创建，Agent 不负责直接创建 `apps` 目录下的新项目。
2. Agent（Claude Code / Codex / OpenClaw）应直接将真实影刀项目目录作为工作根目录打开，而不是把本知识库仓库当成业务项目目录。
3. Agent 在真实影刀项目目录中开发和修改代码；本仓库只用于查规则、查 API、查示例和查排错方法，不承担业务项目源码目录角色。
4. 影刀项目代码入口通常为 `main(args)`；常见初始化导入包括 `import xbot`、`from . import package`、`from .package import variables as glv`。
5. `args` 中使用的流程参数名必须由人类先在影刀编辑器中配置，Agent 不应自行猜测参数名。
6. 开发完成后，代码不会自动进入影刀编辑器，必须从本仓库根目录调用 `shadowbot_sync_tool.py`，并指定目标影刀项目目录进行同步。
7. 同步前先核对：`main(args)` 入口结构是否正确、`args` 参数名是否已配置、`package.variables` 变量名是否一致、元素库名称和资源文件名称是否一致、目标项目环境是否已安装所需市场指令。
8. 测试由人类在影刀编辑器中完成；Agent 根据测试反馈继续修改并再次同步，不把“已同步”写成“已在编辑器内验证通过”。
9. `shadowbot_sync_tool.py prepare` 只接收 `.py` 文件作为输入；不要把 `package.json` 之类非 Python 文件直接传给 `prepare`。
10. `prepare` 会顺带处理 `package.json` 的 flow 更新和编译，但不会自动备份项目；如果只改了 Python 文件，正常只传相关 `.py` 文件即可。
11. 如需备份，请显式调用 `shadowbot_sync_tool.py backup ...`，不要把 `prepare` 当成自动备份命令。

最小调用示例：

```powershell
python .\shadowbot_sync_tool.py --project-dir "%LOCALAPPDATA%\ShadowBot\users\<user_id>\apps\<app_id>" prepare main.py
```

## 文档维护约定

1. 本仓库没有统一的构建、启动或测试命令；主要工作流是阅读文档并在影刀环境中验证。
2. 先查文档，再写代码；文档不明确时不要猜。
3. 新增方案应尽量写清业务背景、推荐分工、关键步骤和注意事项。
4. 文档中的 API 引用应链接到 `xbot-api-docs/docs/` 对应文档。
5. 本项目文档主要由历史资料、源码分析、人工整理和 AI 辅助生成，不保证与实际状态始终完全一致。
6. 如果发现文档描述与实际代码行为不符、接口或参数变化、目录结构或配置项变化、文档存在错误遗漏过期内容、或项目新增重要功能但文档尚未覆盖，应记录为待补充事项。
7. 优先完成当前开发任务，不要中断当前工作流去直接改文档；在任务完成后统一汇总问题。
8. 汇报文档差异时，建议包含：发现位置、当前文档描述、实际情况、建议修改内容。
9. 如发现文档与项目实际情况存在差异，主动询问：`发现文档与项目实际情况存在差异，是否需要我同步更新相关文档？`

## LLM Wiki 维护规则

1. 本仓库文档分三层维护：`llms.txt` 是入口导航层，`wiki/` 是整理记录层，`xbot-api-docs/docs/` 是稳定文档层。
2. `llms.txt` 只做仓库定位、规则入口和核心文档导航，不重复完整规则，不承载细节结论。
3. `xbot-api-docs/docs/` 优先保存经过整理、来源明确、相对稳定、可直接作为开发依据的 API 说明。
4. `wiki/` 用于沉淀跨文档总结、常见坑点、历史纠错和待验证事项，不替代稳定文档主库。
5. 新增资料时，不要直接覆盖原始结论；先记录来源、当前判断和验证状态。
6. 发现历史结论错误时，写入 `wiki/error-book.md`，说明错误说法、正确说法、依据和影响范围。
7. 发现暂时无法确认的 API 行为、参数差异、源码差异或业务规则差异时，写入 `wiki/unresolved.md`。
8. 不确定的 API 行为必须标注“需运行验证”，不要包装成确定结论。
9. 确认无误后，再同步更新 `xbot-api-docs/docs/` 中的对应稳定文档。
10. 不要把真实项目路径、账号、token、Cookie 或其他敏感信息写入 Wiki。

## 新增规则维护流程

1. 先判断内容属于入口导航、整理记录，还是稳定规则 / 稳定文档。
2. 入口导航改 `llms.txt`；纠错、坑点、待验证内容先记到 `wiki/`；稳定规则或稳定 API 说明写 `AGENTS.md` 或 `xbot-api-docs/docs/`。
3. 暂时不能确认的内容必须标注“需运行验证”，不要直接写成确定结论。
4. `wiki/` 中的结论确认无误后，再同步回 `AGENTS.md`、`xbot-api-docs/docs/` 或 `llms.txt`。
