# AGENTS.md

本文件是影刀 xbot / 编码版项目的通用 Agent 规则，适用于后续多个影刀项目复用。

核心目标：**最小改动、简单直接、可读可维护、少工程化、不猜 API、不做无关重构。**

---

## 1. 核心目标

1. 优先保证代码能运行，再考虑优化。
2. 优先采用简单、直接、容易审查的实现。
3. 只解决当前任务明确要求的问题。
4. 不为“高级”“规范”“通用”而引入复杂设计。
5. 不提前处理未出现、未验证、低概率的场景。
6. 不做与当前需求无关的重构、改名、格式化或结构调整。
7. 每一处改动都必须能对应当前需求。
8. 发现潜在问题可以指出，但不要擅自扩大修改范围。

---

## 2. 工作目录与知识库

1. 真实业务代码应在影刀项目目录中修改，不要把知识库目录当成业务项目根目录。
2. 影刀 xbot / AI 开发知识库通常位于 `~\Desktop\影刀xAI开发指南`，用于查规则、查 API、查示例和调用同步工具。
3. 写影刀编码版代码前，优先查知识库中的 `xbot-api-docs`。
4. 不确定 API 行为时，先查当前项目已有代码，再查文档或源码；必要时用 `inspect.signature()`、`inspect.getfile()` 看真实实现。
5. 如果仍不确定，必须标注“需运行验证”，不要自行推断。
6. 命令异常时先判断是否是路径、编码、Shell 或运行环境问题，不要直接判断为项目失败。

---

## 3. 修改前判断

修改代码前先明确：

1. 当前问题是什么。
2. 证据来自哪里：用户描述、报错、当前代码、文档、页面或源码。
3. 哪些文件 / 函数需要改。
4. 哪些内容不能改。
5. 最小可行修改是什么。
6. 修改后如何验证。

处理 Bug 时按顺序：

1. 先复现或定位问题。
2. 再找当前代码中的确定证据。
3. 只修改与问题直接相关的位置。
4. 修改后说明验证方式。
5. 不要把旧项目经验或未确认框架行为套进当前问题。

---

## 4. 代码风格

1. 代码要简单、直接、容易阅读。
2. 优先顺序式流程，数据流显式，关键状态清楚。
3. 变量名直白；临时变量可短但要直观，例如 `data`、`item`、`records`。
4. 不要使用 `x`、`tmp`、`obj` 这类过于抽象的变量名。
5. 保持现有项目风格，不要无关格式化整个文件。
6. 代码关键步骤写中文注释，解释业务意图、关键判断和关键流程。
7. 不要给简单赋值、基础语法或明显逻辑写无意义注释。
8. 函数注释使用 Sphinx / reStructuredText 格式。
9. 默认使用标准库；第三方依赖需用户明确允许或项目已使用。
10. Git 提交信息、阶段性备注和变更摘要优先使用中文。
11. `run.py` 作为主入口文件，应保持简洁，只放主流程和必要调度逻辑。
12. 工具方法、复用逻辑和辅助函数优先放到 `tools.py`；简单单次逻辑可以直接内联在当前流程中。

---

## 5. 函数、封装与解耦

1. 默认不要为了“看起来规范”拆函数。
2. 单次调用、函数体很短、内联后更直观的逻辑，直接写在调用处。
3. 函数体只有 1 条语句时，默认不要封装。
4. 抽函数后需要更多参数传递、阅读更绕时，不要抽。
5. 只有逻辑较长、明显重复、代表明确业务步骤，或项目已有同类结构时，才抽函数。
6. 默认不要新增类、管理器、复杂配置层或抽象层。
7. 不提前设计扩展点、兼容层、多平台层、多通知渠道层。
8. 用户没有要求长期架构时，保持最小可用实现。
9. 函数优先接收实际需要的具体值；只用 dict 少数字段时，不要把整个 dict 传进去再拆。
10. 返回值优先只承载主业务结果，不夹带可现场计算的统计、日志、通知汇总。
11. 缓存、通知、统计、日志不要无意义耦合到同一个复杂结构里。
12. 如果后置信息可以从主结果推导，就在最终使用处计算。

---

## 6. 输入、参数与异常

1. 调用方已经约定入参类型或记录结构时，业务逻辑直接按约定使用。
2. 不要在业务逻辑中反复写 `isinstance`、`dict(...)`、空结构兜底等防御性归一化。
3. 输入形态确实不确定时，只在边界统一归一化一次。
4. 不为影刀必然存在的运行环境能力写兜底封装；本地测试需要 stub 时放在测试代码里。
5. 如果用户或现有代码已明确 `args` 参数名，严格按已知参数名使用。
6. 如果不知道需要哪些 `args` 参数，Agent 应自行定义一组最小、直观、稳定的参数名。
7. Agent 定义新 `args` 参数后，最终回复必须列出参数名、含义和建议类型，方便用户在影刀编辑器创建。
8. 来自 `args` 的参数默认按“参数优先、空值回退全局变量”处理：`args.get("xxx") or glv.get("xxx")`。
9. 如果业务明确需要区分“显式传空值”和“未传值”，必须在代码或最终说明中明确写出原因，不要静默改规则。
10. 不要同时兼容多个猜测参数名，例如 `data_list / items / 数据列表` 混用。
11. 异常处理只覆盖当前业务明确需要处理的失败情况。
12. `except` 优先捕获明确异常类型，例如 `ValueError`、`KeyError`、`json.JSONDecodeError`。
13. 捕获异常时必须保留关键错误信息，不要吞异常不说明。
14. 清理、关闭、停止监听等收尾失败，至少记录 `warning`，不要静默 `pass`。
15. 数据写入任务要让失败隔离粒度和保存时机对齐：逐项捕获异常就逐项保存，整批处理才在批量末尾保存。

---

## 7. 日志、影刀 API 与页面自动化

1. 影刀项目写日志时，禁止使用 Python 标准 `logging`。
2. 只允许使用 `from xbot.app import logging`。
3. 出现 `import logging`、`logging.basicConfig(...)`、`logging.getLogger(...)`、`logger.exception(...)` 都视为错误实现。
4. 影刀日志方法只传一个字符串参数，例如 `logging.info(text)`；不要写 `logging.info("x=%s", value)`。
5. 需要动态内容时使用 f-string，例如 `logging.info(f"行数={n}")`。
6. 需要异常堆栈时，在 `except` 里用 `traceback.format_exc()` 拼到 `logging.error(...)`，不要用 `logger.exception(...)`。
7. 影刀项目入口通常为 `main(args)`。
8. 编码版参数、市场指令参数必须以文档或源码为准，不要根据可视化界面中文选项猜测。
9. 可视化界面能跑，不代表编码版参数一致。
10. 涉及浏览器、URL、页面业务时，优先使用 `xbot.web` 及其浏览器对象能力。
11. 除非用户明确要求接口方式，或项目已有接口调用方式，否则不要用 `requests`、`httpx`、`aiohttp`、`urllib` 处理网页业务。
12. 能用元素、选择器、页面接口处理，就不要优先坐标点击。
13. 中文或长文本输入优先考虑剪贴板输入。
14. 页面字段、按钮含义、价格口径必须以当前需求、页面证据或已验证结果为准。
15. XPath、CSS、页面接口、按钮状态未验证时，必须标注“需运行验证”。

---

## 8. 同步与验证

1. 修改已有文件前，先阅读相关上下文。
2. 手工改文件时优先使用补丁或编辑工具做最小 diff，避免无必要地整文件重写。
3. 不要用脚本批量改无关文件；确需批量处理时，先确认匹配范围只包含本次任务需要修改的文件。
4. 工作区可能已有用户改动；不要回滚、覆盖或清理不是本次任务产生的改动。
5. 不要执行 `git reset --hard`、`git checkout --` 等会丢弃改动的命令，除非用户明确要求。
6. 搜索文件名、代码和文本时，优先使用 `rg` / `ripgrep`。
7. 修改 `.py` 文件后，需要运行知识库根目录的 `shadowbot_sync_tool.py prepare` 同步收尾。
8. `shadowbot_sync_tool.py prepare` 只接收 `.py` 文件作为输入，不要把 `package.json` 等非 Python 文件传给它。
9. 同步完成不等于已在影刀编辑器内验证通过。
10. 测试通常由人类在影刀编辑器中完成；Agent 根据测试反馈继续修改。
11. 运行验证命令后，最终回复要说明关键结果，不要假设用户能看到终端输出。

---

## 9. 禁止行为

禁止以下行为：

1. 无关重构、无关格式化、无关改名。
2. 擅自更换实现路线。
3. 引入不必要依赖。
4. 引入复杂封装、复杂抽象层、复杂配置系统。
5. 为低概率场景写大量防御代码。
6. 隐藏副作用。
7. 吞异常不说明。
8. 基于旧项目经验做推测。
9. 修改与当前需求无关的代码。
10. 编造不存在的 API、命令、目录、文件、XPath。
11. 自行猜测页面字段、按钮含义、价格口径。
12. 同时兼容多个未经确认的参数名。
13. 自行猜测市场指令编码版参数。
14. 提交账号、密码、token、webhook、敏感路径。
15. 把“需运行验证”的内容描述成“已经验证通过”。

---

## 10. 最终回复要求

每次完成代码修改后，最终回复需要包含：

1. 修改了哪些文件。
2. 每个文件改了什么。
3. 是否符合最小改动。
4. 如何验证。
5. 哪些内容仍需运行验证。
6. 是否有未完成事项。

如果没有实际运行测试，必须明确说明“未运行测试”。

不要把静态检查、代码阅读、同步完成描述成真实影刀编辑器内验证通过。
