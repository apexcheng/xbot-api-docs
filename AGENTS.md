# AGENTS.md

本文件是本知识库唯一的常驻 Agent 规则源。API 签名、参数和返回值属于 `xbot-api-docs/`；详细示例和低频边界按本文链接读取，不在这里重复展开。

核心原则：**最小改动、过程清楚、不猜 API、按风险验证。**

## 1. 优先级与边界

1. 用户当前需求和真实影刀项目中的业务逻辑、参数约定、数据口径优先。
2. 用户明确给出的 API 调用方式、XPath、字段名、参数名、路径、Sheet 名和业务规则视为已确认输入。除非用户要求核验，或真实代码、源码、运行证据出现直接冲突，否则不得擅自替换。
3. 知识库只补足用户和真实项目无法确认的部分，不覆盖已确认事实。发现冲突且会影响实现时，停止相关修改并说明冲突。
4. 只修改当前需求直接相关的内容，不做无关重构、格式化、改名或结构调整；保留用户已有改动，不执行会丢失改动的命令。
5. 本仓库是知识库，不是真实影刀应用目录；业务代码应在用户指定的真实项目中修改。
6. README 和 `llms.txt` 只负责介绍与导航；根规则、编程风格说明和 API 事实各自只维护一份。
7. 规则冲突且没有更高优先级要求时，选择改动更小、结构更简单、业务顺序更清楚、风险更低的方案。

## 2. 默认执行

1. 目标清楚时直接执行，不先写长计划或反复确认。
2. 只有需求或取舍不清楚、新增完整功能、跨模块影响主流程、涉及当前代码无法确认的外部行为，或涉及批量写入、删除、迁移、发布、权限等高风险操作时，才做简短的“计划 → 执行 → 验证”。
3. 能在现有结构完成就不新增文件、依赖、配置层或抽象层；每一处改动都应能追溯到当前需求。
4. 不为单次任务创建状态文件或临时进度文档；长期业务规则和设计决策写入项目已有正式文档。

## 3. 代码结构与编程风格

本节是业务代码的硬约束，不因详细说明移到其他页面而减弱。

1. 影刀 RPA、自动化脚本和数据处理默认使用自上而下的过程式主流程，让读取、判断、操作、写回、保存和通知顺序直接可见。业务步骤有明确先后时，代码保持同样顺序。
2. `run.py` 保留 `main(args)` 入口和主业务顺序；`config.py` 只放项目配置、配置路径或稳定静态设置；稳定且真正复用的能力沿用项目已有 `tool.py`、`utils.py`、`win_utils.py`，不为整理代码另建工具层。
3. 单次使用的 URL、XPath、CSS、字段名、按钮文案、状态值、简单属性、简单表达式和立即调用结果，默认在使用位置内联。只有需要复用、后续判断、表达式较复杂、命名能解释业务含义，或必须保留对象状态时才提取变量。
4. 单次使用的短逻辑和一两行的业务步骤默认就地表达，不因为“有独立业务含义”就拆函数。只有逻辑重复，或代码较长、业务边界清晰且抽出后主流程明显更易读时才提取函数或模块。
5. 函数只接收实际需要的具体值；只使用字典中少数字段时，不传整个 `dict` 或巨型 `context`。状态只保留跨步骤确实需要共享的部分。
6. 普通影刀业务流程默认不用类。只有存在明确对象生命周期、需要长期维护实例状态，或项目已有同类结构且沿用更简单时才使用类。
7. 优先使用普通变量、`if`、`for`、`try` 等直白写法；不使用会隐藏业务顺序的嵌套推导式、`lambda`、复杂映射、装饰器、生成器、动态调度或多层 wrapper。
8. 不新增无必要的 Service、Manager、Context、Registry、Factory、Dispatcher、Strategy 或大型配置层；不为未来需求预留未使用参数、分支、Hook、兼容层或扩展点。
9. 函数和方法调用在保持可读时写在一行，参数很多或结构复杂时正常换行。变量名直白表达业务含义；只给关键业务判断写中文注释。需要函数文档注释时使用 Sphinx / reStructuredText 格式。
10. 已知参数名时严格使用现有名称；新增 `args` 参数使用最小、直观、稳定的名称；输入形态不确定时只在 `main(args)` 边界归一化一次，不同时兼容多个未经确认的结构。
11. 异常处理只覆盖明确需要处理的失败，不静默吞错。直接继续失败时使用 `raise`；补充业务上下文时使用 `raise ... from exc`；异常路径的收尾不得覆盖原业务异常。
12. 健壮性只处理真实可能且业务明确要求处理的失败。正常业务 API 已能明确抛错时，直接依赖该失败，不增加等价的前置校验、全量预检、重复读取或多套兜底逻辑。
13. 旧代码可以提供线索，但不能证明设计正确。不要继续扩散旧代码中的过度封装、重复防御、无意义中间变量、一次性函数或历史兼容结构。

变量内联、函数边界和异常收尾的完整判断与示例见 `docs/coding-style.md`；本节已足以约束普通小改，不要求每次加载该页面全文。

读取旧版可视化编排项目时，使用真实项目中的 `xbot-visual-flow-reader` Skill；纯 Python 任务不触发。

## 4. API 与知识库

1. 不根据通用 Python 经验、旧项目记忆或名称相似度猜 xbot API、市场指令、页面字段、XPath 或命令。
2. 用户已给出或当前代码已确认的稳定调用，优先原样沿用最小必要部分。只有新增、修改或排查相关行为且现有证据不足时，才按 `llms.txt` 读取对应事实页的相关章节。
3. 网页 DOM、定位、等待、下载、Cookie、Profile 或 iframe 查 `xbot-api-docs/docs/browser.md`；Win32 窗口、元素、图像、焦点或坐标查 `win32.md`；Excel / WPS 写入、精度、占用或生命周期查 `excel.md`。不机械读取与当前改动无关的全文。
4. 真实业务代码中的 URL、路径、Sheet 名、`args` 字段、XPath 和 API 参数必须来自用户、真实项目或可核验证据，不得用假值拼成看似可运行的实现。
5. 文档中的 API Python 片段须说明前置对象和适用条件。含脱敏占位、未知步骤或实际副作用的示例标注“非执行调用说明（不可直接运行）”，不得冒充可运行实现。
6. 网页业务默认使用 `xbot.web`；异步行为等待明确条件，不靠不断增加固定 `sleep`；已有元素或选择器时不优先退回坐标操作。
7. 影刀日志使用 `from xbot.app import logging`。动态内容使用完整 f-string；异常堆栈在 `except` 中使用 `traceback.format_exc()`，不使用 Python 标准 `logging` 或 `logger.exception()`。
8. 显式重试应记录尝试次数和必要上下文，失败时保留完整堆栈；最终失败保留最后异常及其异常链，不静默重试。
9. 必须依赖真实影刀、WPS、浏览器或页面才能确认的行为标记“需运行验证”，不得把推测、静态检查或同步结果写成已验证。
10. 新建或补全编码版项目读取 `docs/base-project-skeleton.md`；多个必需数据源写入同一工作簿读取 `docs/multi-source-report-safety.md`；跨领域排错仍不明确时读取 `docs/troubleshooting.md`。

## 5. 验证与影刀同步

1. 按风险做最小必要验证。小改检查相关路径；多文件、公共逻辑或数据处理改动检查主要调用方；默认不新增测试、不运行无关的完整测试或构建。
2. 删除、迁移、批量写入、发布、权限和生产配置先确认范围与回退方式。验证失败时先区分代码、环境、依赖和路径问题，不为通过无关检查扩大修改。
3. 同一结果已通过正常业务流程获得后，不重复读取或扫描；“需运行验证”只保留给静态证据确实无法确认的行为。
4. `shadowbot_sync_tool.py` 与 `package.json` 位于真实项目根目录。新增 `.py` 文件后默认运行 `python shadowbot_sync_tool.py`；只修改已有 `.py` 文件时，仅在用户明确要求同步时运行。
5. 同步脚本自动扫描项目根目录 Python 文件，不接收文件列表或 `prepare` 子命令。同步不是 Git 操作，成功也不等于已在影刀编辑器中运行验证。

## 6. 安全与汇报

1. 不隐藏副作用，不通过吞异常、静默失败或模糊返回值让主流程看起来成功。
2. 不提交账号、密码、Token、Cookie、Webhook、客户数据或未公开地址；示例、日志和返回值必须脱敏。
3. 完成后简短说明修改内容和实际验证。只有未执行项会影响结果判断，或本应执行但受限时，才特别说明。
4. 新增 `args` 参数时列出参数名、含义和建议类型；Git 提交信息和变更摘要默认使用中文。
