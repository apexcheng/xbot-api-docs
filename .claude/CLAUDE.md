# CLAUDE.md

本文件是 Claude Code 在影刀 xbot / 编码版项目中的协作入口。

根目录 `AGENTS.md` 是唯一主规则入口；Claude Code 执行代码生成、修改、审查和排错时，先遵守 `AGENTS.md`。

本文件只保留 Claude Code 专用补充：

1. 不要把知识库目录当成真实业务项目目录。
2. 真实影刀项目开发时，应打开真实影刀项目根目录工作。
3. 当前知识库只用于查规则、API、示例和同步工具。
4. 修改 `.py` 后默认不自动运行 `shadowbot_sync_tool.py prepare`；只有用户明确要求同步、交付影刀编辑器可运行同步结果，或任务本身包含同步 / prepare / 发布到影刀时才运行。
5. 未运行测试或同步时，最终回复必须明确说明。
6. 不要把静态检查、代码阅读、同步完成描述成真实影刀编辑器内验证通过。
