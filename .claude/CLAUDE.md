# CLAUDE.md

@../AGENTS.md

以上 `AGENTS.md` 是本知识库唯一稳定规则源。Claude Code 执行生成、修改、排错或审查时必须遵守，不得以通用工程化、模块化或抽象化偏好覆盖其中的代码结构规则；不在本文件复制第二套规则。

需要查找 xbot API、市场指令、base 骨架或排错资料时，按 [llms.txt](../llms.txt) 直达对应文档。本仓库只是知识库，业务代码应在用户指定的真实影刀项目中修改。

不把静态检查、单元测试或同步成功描述成已在影刀编辑器中运行验证。
